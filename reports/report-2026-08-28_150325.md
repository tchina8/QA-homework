# Регрессионный прогон — 2026-08-28_150325

Собран автоматически: `workflow/run_regression.py`, шаги 1–5.
Проверка результата и финальная строка — отдельным субагентом, см. раздел «Вердикт».

## 1. Прогон

* команда: `pytest.exe -v --tb=long`
* код возврата: `0`
* длительность: 0.8 с
* сырой вывод: [`reports/raw/2026-08-28_150325.txt`](2026-08-28_150325.txt) (28972 байт)

## 2. Итоги

| Показатель | Значение |
|---|---:|
| Всего тестов | 252 |
| Прошло | 251 |
| Упало | 0 |
| Ожидаемо упало (xfail) | 1 |
| Неожиданно прошло (XPASS) | 0 |
| Пропущено | 0 |

### Разбивка по файлам

| Файл | Всего | Прошло | Упало | xfail | XPASS |
|---|---:|---:|---:|---:|---:|
| `tests/test_annuity.py` | 88 | 88 | 0 | 0 | 0 |
| `tests/test_insurance.py` | 67 | 67 | 0 | 0 | 0 |
| `tests/test_prepayment.py` | 79 | 78 | 0 | 1 | 0 |
| `tests/test_rounding_discipline.py` | 18 | 18 | 0 | 0 | 0 |

## 3. Сравнение с предыдущим прогоном

Предыдущих прогонов нет — сравнивать не с чем. Это базовый прогон.

## 4. Открытые баги и их тесты

| Баг | Статус | Тесты | Живой тест |
|---|---|---|:--:|
| BUG-01 | открыт | `test_prepayment.py` | да |
| BUG-02 | открыт | `test_prepayment.py` | да |
| BUG-03 | устранён | — | **НЕТ** |
| BUG-04 | устранён | — | **НЕТ** |
| BUG-05 | устранён | — | **НЕТ** |
| BUG-06 | устранён | — | **НЕТ** |
| BUG-07 | открыт | — | **НЕТ** |
| BUG-08 | открыт | — | **НЕТ** |
| BUG-09 | открыт | — | **НЕТ** |
| BUG-10 | открыт | — | **НЕТ** |

> **Открытые баги без теста:** BUG-07, BUG-08, BUG-09, BUG-10. Дефект, зафиксированный только текстом, при следующей правке кода не подаст никакого сигнала.

## Вердикт

СДЕЛАНО: прогон воспроизведён независимо (`.venv\Scripts\pytest.exe`) и сошёлся с сырым выводом — 266 собранных тестов, 265 passed, 1 xfailed, ноль FAILED/ERROR/SKIPPED, код возврата 0; XPASS не появился, строгий `xfail(strict=True)` на BUG-01 (`test_penny_prepayment_on_long_loan_closes_the_schedule`) продолжает держать дефект и упадёт при молчаливой починке; число тестов не уменьшилось — коммит `5c8a2ad` только добавил один тест (265 → 266), удалённых тестов в diff нет; все шесть артефактов шагов 1–5 на месте.

НЕ ПОЛУЧИЛОСЬ: шаг 2 разобрал вывод неверно — 252 теста вместо 266 и 251 passed вместо 265, потому что регулярное выражение `LINE` в `workflow/run_regression.py` требует node id без пробелов (`(?P<test>\S+)`) и молча пропускает строки с пробелом в id параметризации: выпали ровно 14 тестов — 7 × `test_invalid_loan_is_rejected` и 7 × `test_float_arguments_are_rejected` в `test_annuity.py`, то есть вся группа проверок отказа на некорректный вход; шаг 2 не сверяет свой счёт ни со строкой `collected 266 items`, ни с итоговой строкой `265 passed, 1 xfailed`, поэтому заниженные числа прошли в отчёт (раздел «Итоги» и разбивка по файлам в `report-2026-08-28_150325.md` неверны); шаг 3 не сравнил ничего (`previous: null`, прогонов на диске больше нет — «тесты не пропали» подтверждено git-историей, а не артефактами workflow); признак `has_live_test` в шаге 4 — лишь поиск подстроки «BUG-NN» в исходниках тестов, поэтому «живой тест» BUG-02 — это строка `reason` у xfail-теста BUG-01, отдельного теста на BUG-02 нет; зелёный прогон качества не доказывает — `findings.md` сам фиксирует слепую зону: все эталонные примеры спеки 12-месячные, ветка отрицательного запаса последнего платежа не покрыта (падение на 42 % конфигураций найдено расчётом, а не тестами), BUG-07…BUG-10 не покрыты ничем, а после потери 14 негативных тестов разбором отчёт занижал покрытие именно там, где оно и так тонкое.

ОСТАЛОСЬ: человеку решить, какое из двух требований уступает в противоречии И-10 и раздела 4.2 (BUG-02) — до этого BUG-01 чинить нельзя, правильное поведение не определено; завести тесты на открытые BUG-07…BUG-10 или сознательно принять риск; починить разбор в шаге 2 и добавить обязательную сверку с итоговой строкой pytest и с `collected`, чтобы молчаливая потеря тестов не повторилась и не делала прогон зеленее, чем он есть; добавить в спеку четвёртый эталонный пример на длинном сроке с отрицательным запасом последнего платежа; поправить битую ссылку на сырой вывод в отчёте (ведёт на `reports/2026-08-28_150325.txt` вместо `reports/raw/2026-08-28_150325.txt`).
