"""Кредитный калькулятор — объект тестирования для регресса (ДЗ №2, вариант B4).

Источник истины — `docs/spec.md`. Любое расхождение между кодом и спекой
решается в пользу спеки: сначала правится спека, потом код.

Модули:
  * `calc.money`      — `Decimal` из строк и единственная функция округления (раздел 1.1);
  * `calc.annuity`    — аннуитетный платёж и график (разделы 3.1–3.4, 4.3);
  * `calc.prepayment` — досрочка: сокращение срока и уменьшение платежа (раздел 3.6);
  * `calc.insurance`  — страховка из своих средств и в кредит (раздел 3.5).
"""

from __future__ import annotations

__all__ = ["annuity", "insurance", "money", "prepayment"]
